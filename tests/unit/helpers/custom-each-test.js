import { customEach } from 'rappi-ember/helpers/custom-each';
import { module, test } from 'qunit';

module('Unit | Helper | custom each');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = customEach([42]);
  assert.ok(result);
});
